server:
  port: 8080

spring:
  application:
    name: student
  datasource:
    password: 74108520
    url: jdbc:postgresql://localhost:5432/student
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true